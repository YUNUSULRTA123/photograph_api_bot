# Telegram Image Generator Bot (Reve API)

Этот Telegram-бот позволяет генерировать изображения по текстовому описанию (промпту), используя API сервиса **Reve**. 

## 🚀 Функционал
* Генерация изображений в формате 16:9 по текстовому запросу.
* Автоматическая обработка и декодирование Base64 ответов от API.
* Логирование работы бота в консоль.
* Простая обработка ошибок (тайм-ауты, ошибки API).

## 🛠 Технологии
* **Python 3.x**
* **pyTelegramBotAPI** — для взаимодействия с Telegram.
* **Requests** — для работы с HTTP-запросами к Reve API.

## 📋 Инструкция по установке

### 1. Подготовка окружения
Убедитесь, что у вас установлен Python. Сначала клонируйте репозиторий или скопируйте файлы, затем установите необходимые библиотеки:

```bash
pip install pyTelegramBotAPI requests
```

### 2. Настройка конфигурации
Создайте в корневой папке файл `config.py` и добавьте в него свои ключи доступа:
```python
# config.py
TELEGRAM_BOT_TOKEN = "ВАШ_ТОКЕН_ОТ_BOTFATHER"
REVE_API_KEY = "ВАШ_API_КЛЮЧ_ОТ_REVE"
```

### 3. Запуск
Запустите бота командой:
```python
python main.py
```
## 🖥 Использование
1) Напишите боту `/start`.
2) Отправьте любое текстовое описание (например: *"Futuristic city with neon lights, digital art"*).
3) Подождите несколько секунд, пока бот сгенерирует и отправит вам изображение.

## ⚠️ Примечание
Бот сохраняет последнее сгенерированное изображение под именем `result.png` в корневой директории. При каждом новом запросе файл перезаписывается.

---

### Совет по улучшению кода для README:
Сейчас бот сохраняет все картинки под одним именем `result.png`. Если два пользователя напишут одновременно, может возникнуть конфликт.
